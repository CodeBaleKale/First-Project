<resources>
    <!-- Compose draws everything; this only styles the window before setContent. -->
    <style name="Theme.SnapCal" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FAF7F2</item>
        <item name="android:statusBarColor">#FAF7F2</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
